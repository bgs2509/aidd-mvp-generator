# =============================================================================
# {project_name} MVP — Переменные окружения
# =============================================================================
#
# Скопируйте этот файл в .env и заполните значения:
#   cp .env.example .env
#
# ВАЖНО: Никогда не коммитьте .env файл в репозиторий!
# =============================================================================

# === Общие настройки ===
ENVIRONMENT=development
# Опции: development, staging, production

# SECURITY: В production ВСЕГДА должно быть false!
DEBUG=false
# true/false — включить отладочный режим (только для разработки)

LOG_LEVEL=INFO
# Опции: DEBUG, INFO, WARNING, ERROR

# === Порты сервисов ===
API_PORT=8000
DATA_API_PORT=8001
BOT_WEBHOOK_PORT=8443

# === PostgreSQL ===
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=app
# SECURITY: Замените на надёжный пароль! Минимум 16 символов, буквы + цифры + спецсимволы
# Генерация: openssl rand -base64 24
POSTGRES_PASSWORD=CHANGE_ME_IMMEDIATELY
POSTGRES_DB={context}_db

# Полный URL для подключения (автоматически собирается)
# DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# === Redis (опционально) ===
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
# Оставьте пустым если без пароля

# REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}/0

# === Telegram Bot (если используется) ===
# SECURITY: Получите токен от @BotFather и замените значение
TELEGRAM_BOT_TOKEN=CHANGE_ME_IMMEDIATELY
# Получить: https://t.me/BotFather

TELEGRAM_WEBHOOK_URL=https://your-domain.com/webhook
# URL для webhook режима (production)

# SECURITY: Генерация секрета: openssl rand -hex 32
TELEGRAM_WEBHOOK_SECRET=CHANGE_ME_IMMEDIATELY
# Секрет для валидации webhook

# === JWT / Auth (если используется) ===
# SECURITY: Генерация ключа: openssl rand -hex 32
# ВАЖНО: Должен быть минимум 32 символа!
JWT_SECRET_KEY=CHANGE_ME_IMMEDIATELY_MIN_32_CHARS
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# === External APIs ===
# DATA_API_URL=http://{context}-data:8001
# BUSINESS_API_URL=http://{context}-api:8000

# === Sentry (мониторинг ошибок) ===
SENTRY_DSN=
# Получить: https://sentry.io

# === Development Tools ===
PGADMIN_EMAIL=admin@example.com
# SECURITY: Замените на надёжный пароль!
PGADMIN_PASSWORD=CHANGE_ME_IMMEDIATELY
PGADMIN_PORT=5050

REDIS_COMMANDER_PORT=8081

# === Production (только для prod) ===
# DOMAIN=your-domain.com
# SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
# SSL_KEY_PATH=/etc/nginx/ssl/key.pem
