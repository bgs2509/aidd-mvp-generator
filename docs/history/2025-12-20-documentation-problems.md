# Проблемы документации AIDD-MVP Generator

> **ARCHIVED**: Все проблемы, перечисленные в этом документе, были исправлены.
> См. [2025-12-21-documentation-master-todo.md](2025-12-21-documentation-master-todo.md)

**Дата анализа**: 2025-12-20
**Обновлено**: 2025-12-21
**Статус**: ✅ ИСПРАВЛЕНО (ARCHIVED)
**Проанализировано файлов**: 103+

---

## Классификация проблем

| Приоритет | Описание | Количество |
|-----------|----------|------------|
| 🔴 CRITICAL | Блокирует работу пайплайна | 13 |
| 🟠 HIGH | Приводит к некорректному поведению | 10 |
| 🟡 MEDIUM | Вызывает путаницу | 9 |
| 🟢 LOW | Косметические несоответствия | 4 |

**ИТОГО: 36 проблем** (было 31, добавлено 5 новых критических)

---

## 🔴 CRITICAL: Критические проблемы

### P-001: Несуществующие директории для артефактов

**Тип**: Целостность структуры
**Файлы**: `workflow.md`, `CLAUDE.md`, `.claude/agents/*.md`

**Описание**:
Документация ссылается на директории, которые не существуют в репозитории:

```
docs/prd/           ← НЕ СУЩЕСТВУЕТ
docs/architecture/  ← НЕ СУЩЕСТВУЕТ
docs/plans/         ← НЕ СУЩЕСТВУЕТ
docs/reports/       ← НЕ СУЩЕСТВУЕТ
docs/tasklists/     ← НЕ СУЩЕСТВУЕТ
```

**Фактическая структура**:
```
docs/
├── history/
└── templates/
```

**Затронутые ссылки**:
| Документ | Строка | Ссылка |
|----------|--------|--------|
| `workflow.md` | 80 | `docs/prd/{name}-prd.md` |
| `workflow.md` | 126 | `docs/architecture/{name}-plan.md` |
| `workflow.md` | 184 | `docs/reports/review-report.md` |
| `.claude/agents/analyst.md` | 40 | `docs/prd/{name}-prd.md` |
| `.claude/agents/qa.md` | 41 | `docs/reports/qa-report.md` |

**Влияние на пайплайн**:
- Аналитик не сможет создать PRD в указанной директории
- Архитектор не сможет создать план
- Все агенты, создающие отчёты, не найдут целевые директории

---

### P-002: Неправильный путь к шаблону PRD

**Тип**: Битая ссылка
**Файлы**: `.claude/agents/analyst.md`

**Описание**:
Агент Аналитик ссылается на несуществующий шаблон:

```markdown
# В .claude/agents/analyst.md (строка 136)
| `docs/prd/template.md` | Шаблон PRD |
```

**Фактическое расположение**:
```
docs/templates/prd-template.md  ← Реальный файл
```

**Затронутые документы**:
| Документ | Ссылается на |
|----------|--------------|
| `.claude/agents/analyst.md` | `docs/prd/template.md` |
| `docs/history/2025-12-19-aidd-mvp-framework-plan.md` | `docs/prd/template.md` |
| `docs/history/2025-12-19-aidd-mvp-implementation-todo.md` | `docs/prd/template.md` |

**Влияние на пайплайн**:
- Аналитик не найдёт шаблон для создания PRD
- Генерируемые PRD могут не соответствовать ожидаемому формату

---

### P-003: Конфликт инструментов между агентами и командами

**Тип**: Конфликт документов
**Файлы**: `.claude/agents/*.md`, `.claude/commands/*.md`

**Описание**:
Агенты и команды объявляют разные наборы инструментов для одних и тех же ролей:

**Аналитик (analyst)**:
```yaml
# .claude/agents/analyst.md
tools: Read, Glob, Grep, Edit, Write

# .claude/commands/idea.md
allowed-tools: Read(*), Glob(*), Grep(*), Edit(**/*.md), Write(**/*.md)
```
Конфликт: Агент имеет полный Edit/Write, команда ограничивает только markdown.

**Реализатор (implementer)**:
```yaml
# .claude/agents/implementer.md
tools: Read, Glob, Grep, Edit, Write, Bash

# .claude/commands/generate.md
allowed-tools: Read(*), Glob(*), Grep(*), Edit(**), Write(**), Bash(make :*), Bash(docker :*), Bash(pytest :*)
```
Конфликт: Агент имеет неограниченный Bash, команда ограничивает только make/docker/pytest.

**QA (qa)**:
```yaml
# .claude/agents/qa.md
tools: Read, Glob, Grep, Bash, Edit, Write

# .claude/commands/test.md
allowed-tools: Read(*), Glob(*), Grep(*), Bash(pytest :*), Bash(make :*), Edit(**/*.md), Write(**/*.md)
```
Конфликт: Агент имеет полный Bash, команда ограничивает pytest/make.

**Влияние на пайплайн**:
- Неясно, какой набор инструментов применяется
- Реализатор не сможет выполнить `mkdir`, `cp` для создания структуры
- Возможные ошибки доступа при выполнении команд

---

### P-004: Отсутствие алгоритма отката при неудаче ворот

**Тип**: Неполнота пайплайна
**Файлы**: `workflow.md`

**Описание**:
Документ описывает сценарий "Откат при неудаче" (строки 396-408), но не определяет:

1. Как именно перезапустить этап после исправления?
2. Сохраняются ли результаты предыдущих этапов?
3. Нужна ли повторная валидация всех ворот?
4. Какой этап перезапускать при частичной неудаче?

**Пример из документации (строки 401-407)**:
```
/validate
→ ❌ QA_PASSED: Coverage 68%
→ Добавить тесты
/test
→ ✓ QA_PASSED: Coverage 76%
/validate
```

**Неотвеченные вопросы**:
- После добавления тестов нужен ли `/review`?
- Если изменился код, остаётся ли `REVIEW_OK` в силе?
- Как отслеживать, какие ворота нужно перепроверить?

**Влияние на пайплайн**:
- Неясный порядок действий при неудаче
- Возможный пропуск необходимых проверок
- Риск деплоя кода с непройденными воротами

---

### P-019: Отсутствие индекса/реестра файлов проекта (File Discovery)

**Тип**: Целостность пайплайна
**Файлы**: Весь проект

**Описание**:
В проекте aidd-mvp-generator **отсутствует индексный файл** со списком всех файлов и их назначением:

```
❌ docs/INDEX.md         — НЕ СУЩЕСТВУЕТ
❌ knowledge/INDEX.md    — НЕ СУЩЕСТВУЕТ
❌ .file-registry.json   — НЕ СУЩЕСТВУЕТ
```

**Сравнение с .ai-framework/**:
```
✓ .ai-framework/docs/INDEX.md       — СУЩЕСТВУЕТ (отдельный проект)
✓ .ai-framework/docs/LINKS_REFERENCE.md — СУЩЕСТВУЕТ
```

**Проблема обнаружения файлов в пайплайне**:

| Этап | Агент | Нужен файл | Как найти? |
|------|-------|------------|------------|
| 2 | Исследователь | PRD из этапа 1 | Glob("docs/prd/*.md")? |
| 3 | Архитектор | PRD + результаты исследования | Где результаты? |
| 4 | Реализатор | План | Glob("docs/architecture/*.md")? |
| 5 | Ревьюер | Код | Glob("services/*")? |

**Влияние на пайплайн**:
- AI должен угадывать где искать артефакты
- Нет гарантии нахождения нужных файлов
- Возможны ошибки при множественных файлах

---

### P-020: Нет механизма передачи состояния между этапами пайплайна

**Тип**: Целостность пайплайна
**Файлы**: `workflow.md`, все команды

**Описание**:
Философия "Артефакты = Память" (workflow.md:6) **не реализована**:

```
"Философия": Артефакты = Память. Не полагаемся на память чата.

НО: Нет механизма хранения состояния!
```

**Отсутствующие механизмы**:

| Механизм | Статус | Что нужно |
|----------|--------|-----------|
| Файл состояния | ❌ Нет | `.pipeline-state.json` |
| Пройденные ворота | ❌ Нет | `gates: {PRD_READY: true, ...}` |
| Текущий этап | ❌ Нет | `current_stage: 3` |
| История изменений | ❌ Нет | `artifact_hashes: {...}` |

**Сценарий проблемы**:
```bash
# Сессия 1:
/idea "проект"       # PRD_READY ✓
/research            # RESEARCH_DONE ✓
/plan               # PLAN_APPROVED ✓ (пользователь утвердил)

# --- Новая сессия чата ---

# Сессия 2:
/generate           # ❓ Откуда знать что PLAN_APPROVED?
                    # ❓ Какой план использовать?
```

**Влияние на пайплайн**:
- Потеря контекста между сессиями
- Невозможность проверить пройденные ворота
- Повторение уже выполненной работы

---

### P-021: Нет алгоритма обнаружения входных артефактов для каждого этапа

**Тип**: Неполнота пайплайна
**Файлы**: `.claude/commands/*.md`, `.claude/agents/*.md`

**Описание**:
Каждая команда требует входные артефакты, но **не определяет алгоритм их поиска**:

**Команда /research** (research.md):
```markdown
## Предусловия
| Ворота | Требование |
| `PRD_READY` | PRD документ создан |

❌ НО: Где искать PRD? Какой паттерн?
```

**Команда /generate** (generate.md):
```markdown
## Предусловия
| `PLAN_APPROVED` | План утверждён пользователем |

❌ НО: Как найти план? docs/architecture/*.md или docs/plans/*.md?
```

**Необходимый алгоритм (отсутствует)**:
```python
# Пример того что ДОЛЖНО быть:
def find_prd():
    patterns = [
        "docs/prd/*-prd.md",
        "docs/*-prd.md"
    ]
    for pattern in patterns:
        files = glob(pattern)
        if files:
            return most_recent(files)
    raise ArtifactNotFound("PRD")
```

**Влияние на пайплайн**:
- Агенты не знают где искать входные данные
- Возможен пропуск актуальных артефактов
- Использование устаревших версий файлов

---

### P-032: Дублирование фреймворков (.ai-framework vs aidd-mvp-generator)

**Тип**: Архитектурная проблема
**Файлы**: Весь проект, `.ai-framework/`

**Описание**:
В проекте существуют **ДВА ПАРАЛЛЕЛЬНЫХ ФРЕЙМВОРКА**:

```
aidd-mvp-generator/           ← ОСНОВНОЙ ПРОЕКТ (неполный, 8 стадий)
│
└── .ai-framework/            ← ЗРЕЛЫЙ ФРЕЙМВОРК (полный, 7 стадий)
    ├── AGENTS.md             ← Полные инструкции для AI
    ├── ARCHITECTURE.md       ← Архитектурные принципы
    ├── CLAUDE.md             ← Verify Before Act протокол
    ├── docs/
    │   ├── INDEX.md          ← ПОЛНЫЙ ИНДЕКС (392 строки)
    │   ├── reference/
    │   │   ├── ai-navigation-matrix.md  ← МАТРИЦА НАВИГАЦИИ
    │   │   ├── deliverables-catalog.md  ← КАТАЛОГ АРТЕФАКТОВ
    │   │   └── ...
    │   ├── atomic/           ← 157+ атомарных документов
    │   └── ...
    └── ...
```

**Проблема**: `.ai-framework/` содержит **ВСЁ**, что отсутствует в основном проекте:

| Компонент | В `.ai-framework/` | В `aidd-mvp-generator/` |
|-----------|--------------------|-----------------------|
| INDEX.md | ✅ 392 строки | ❌ Отсутствует |
| Navigation Matrix | ✅ Полная | ❌ Отсутствует |
| Deliverables Catalog | ✅ Есть | ❌ Отсутствует |
| Атомарная документация | ✅ 157+ файлов | ⚠️ Частичная |
| Уровни зрелости | ✅ 4 уровня (1-4) | ⚠️ Только Level 2 |

**Влияние на пайплайн**:
- Критическая документация существует, но НЕ ИСПОЛЬЗУЕТСЯ
- Основной проект — незавершённая копия
- AI не имеет доступа к полной документации

---

### P-033: Несогласованность количества стадий (7 vs 8)

**Тип**: Конфликт документов
**Файлы**: `workflow.md`, `.ai-framework/docs/reference/ai-navigation-matrix.md`

**Описание**:
Основной проект использует **8 стадий**, а `.ai-framework/` использует **7 стадий**:

**aidd-mvp-generator (8 стадий)**:
```
1. Идея/PRD       → PRD_READY
2. Исследование   → RESEARCH_DONE
3. Архитектура    → PLAN_APPROVED
4. Реализация     → IMPLEMENT_OK
5. Ревью          → REVIEW_OK
6. QA             → QA_PASSED
7. Валидация      → ALL_GATES_PASSED
8. Деплой         → DEPLOYED
```

**.ai-framework (7 стадий с под-стадиями)**:
```
0. Initialization
1. Prompt Validation
2. Requirements Clarification & Intake
3. Architecture Mapping & Planning
4. Generation (4.1, 4.1b, 4.1c, 4.1d, 4.2, 4.2b, 4.3, 4.3b, ...)
5. Quality Verification
6. QA Report & Handoff
```

**Несовместимость**:
- Нет прямого отображения стадий друг на друга
- Разные ворота качества
- Разные критерии перехода

**Влияние на пайплайн**:
- Невозможно использовать оба фреймворка вместе
- Документация противоречит друг другу
- AI получает конфликтующие инструкции

---

### P-034: Несогласованность путей артефактов (docs/ vs ai-docs/)

**Тип**: Конфликт документов
**Файлы**: `roles/validator/artifact-verification.md`, `.ai-framework/`

**Описание**:
Разные части проекта используют **РАЗНЫЕ ПРЕФИКСЫ ПУТЕЙ**:

**В `roles/validator/artifact-verification.md`**:
```markdown
| PRD | `ai-docs/docs/prd/{name}-prd.md` | Анализ | Да |
| Architecture | `ai-docs/docs/architecture/{name}-arch.md` | Архитектура | Да |
```

**В `workflow.md` и других документах**:
```markdown
| PRD | `docs/prd/{name}-prd.md` |
| Architecture | `docs/architecture/{name}-plan.md` |
```

**Несоответствие суффиксов**:
```
workflow.md:           {name}-plan.md
artifact-verification: {name}-arch.md
```

**Влияние на пайплайн**:
- AI не найдёт артефакты по неверным путям
- Валидатор будет искать в `ai-docs/`, где ничего нет
- Разные агенты создают файлы в разных местах

---

### P-035: .ai-framework не интегрирован как submodule

**Тип**: Архитектурная проблема
**Файлы**: `.ai-framework/AGENTS.md`

**Описание**:
Согласно `.ai-framework/AGENTS.md`, фреймворк предназначен для использования как **submodule**:

```markdown
# Framework-as-Submodule Model
The AIDD framework is added as a submodule to client projects:
├── client-project/
│   ├── .ai-framework/ (this framework as submodule)
```

**Проблемы интеграции**:
1. В `aidd-mvp-generator/` нет настройки submodule
2. Нет инструкций по интеграции
3. Нет ссылок из основного проекта на `.ai-framework/`
4. CLAUDE.md основного проекта не включает `.ai-framework/`

**Влияние на пайплайн**:
- AI не знает, что `.ai-framework/` существует
- Зрелая документация игнорируется
- Используется неполный фреймворк

---

### P-036: AI Navigation Matrix не адаптирована для основного проекта

**Тип**: Неполнота документации
**Файлы**: `.ai-framework/docs/reference/ai-navigation-matrix.md`

**Описание**:
Navigation Matrix из `.ai-framework/` — **ИМЕННО ТО, что нужно** основному проекту, но:

1. Использует 7 стадий вместо 8
2. Ссылается на пути `docs/atomic/` которых нет в основном проекте
3. Требует документы, которые не существуют:
   ```
   • docs/guides/ai-code-generation-master-workflow.md
   • docs/reference/maturity-levels.md
   • docs/atomic/services/fastapi/*.md
   ```

**Что должно быть сделано**:
- Адаптировать Navigation Matrix для 8-стадийного процесса
- Создать аналогичные документы или ссылки
- Обновить пути к существующим файлам

**Влияние на пайплайн**:
- AI не может использовать Navigation Matrix напрямую
- Отсутствует критическая документация для навигации

---

### P-022: Нет bootstrap процесса для создания структуры директорий

**Тип**: Неполнота инициализации
**Файлы**: Все, использующие docs/prd/, docs/architecture/, docs/reports/

**Описание**:
Пайплайн предполагает существование директорий, но **не создаёт их**:

**Ожидаемая структура** (workflow.md:299-318):
```
docs/
├── prd/              ← Кто создаёт?
├── architecture/     ← Кто создаёт?
├── plans/            ← Кто создаёт?
├── reports/          ← Кто создаёт?
├── tasklists/        ← Кто создаёт?
└── rtm.md            ← Кто создаёт?
```

**Реальная структура**:
```
docs/
├── history/          ← Единственная существующая
└── templates/        ← Единственная существующая
```

**Вопросы без ответа**:
1. Кто отвечает за создание структуры — Аналитик? Система?
2. Когда создаётся — при первом запуске? При /idea?
3. Нужна ли команда `/init` или `/bootstrap`?

**Влияние на пайплайн**:
- Первая команда /idea завершится ошибкой "директория не найдена"
- Агенты могут создавать файлы в неправильных местах
- Несогласованная структура между проектами

---

## 🟠 HIGH: Проблемы высокого приоритета

### P-005: Дублирование ответственности QA и Ревьюера

**Тип**: Дублирование информации
**Файлы**: `.claude/agents/qa.md`, `.claude/agents/reviewer.md`

**Описание**:
Обе роли проверяют одни и те же аспекты:

| Аспект | Ревьюер (reviewer.md) | QA (qa.md) |
|--------|----------------------|------------|
| DRY | Строки 70-77 | Неявно через тесты |
| KISS | Строки 78-81 | Неявно через тесты |
| YAGNI | Строки 83-86 | Неявно через тесты |
| Качество кода | Строки 68-86 | Строки 17-23 |

**Конфликтный сценарий**:
Что если реализован избыточный функционал, покрытый тестами?
- Ревьюер проверяет YAGNI → должен заметить
- QA проверяет покрытие → тесты проходят
- Кто ответственен за блокировку?

**Влияние на пайплайн**:
- Неясно, кто отвечает за какие проверки
- Возможны пропуски или дублирующие проверки
- Противоречивые решения между этапами

---

### P-006: Отсутствие алгоритма определения режима CREATE/FEATURE

**Тип**: Неполнота пайплайна
**Файлы**: `workflow.md`, `.claude/agents/analyst.md`, `.claude/commands/idea.md`

**Описание**:
Документация описывает два режима (CREATE и FEATURE), но не определяет:

1. **Кто определяет режим?**
   - Аналитик? Система? Пользователь?

2. **Как определяется режим?**
   - В `researcher.md` (строки 49-53) есть намёк:
     ```
     Если существует src/ или services/:
       → Режим FEATURE
     Иначе:
       → Режим CREATE
     ```
   - Но это в Исследователе, а не в Аналитике (первый этап)

3. **Когда определяется режим?**
   - На этапе `/idea` или `/research`?

**Конфликт между документами**:
| Документ | Где определяется режим |
|----------|------------------------|
| `CLAUDE.md` | Неявно через команду `/idea` |
| `workflow.md` | На этапе 2 (Исследование) |
| `.claude/commands/idea.md` | Аналитик определяет |

**Влияние на пайплайн**:
- Неясно, какой процесс запускать
- Аналитик может создать неправильный тип PRD
- Несогласованность между этапами

---

### P-007: Несогласованные пути артефактов

**Тип**: Конфликт документов
**Файлы**: `workflow.md`, `.claude/agents/architect.md`

**Описание**:
Разные документы указывают разные пути для одних и тех же артефактов:

**Архитектурный план (CREATE)**:
```
workflow.md (строка 126):     docs/architecture/{name}-plan.md
architect.md (строка 40):      docs/architecture/{name}-plan.md
```
✓ Согласовано

**План фичи (FEATURE)**:
```
workflow.md (строка 143):     docs/plans/{feature}-plan.md
feature-plan.md (строка 54):  docs/plans/{feature}-plan.md
architect.md (строка 41):     docs/plans/{feature}-plan.md
```
✓ Согласовано, но директория не существует

**RTM (матрица трассировки)**:
```
workflow.md (строка 248):     docs/rtm.md
analyst.md (строка 41):       docs/rtm.md (секция требований)
```
⚠️ Один файл для всего проекта vs секция

**Влияние на пайплайн**:
- Агенты могут создавать файлы в разных местах
- Сложность поиска артефактов
- Возможные конфликты при merge

---

### P-008: Неполная документация команды /feature-plan

**Тип**: Неполнота документации
**Файлы**: `.claude/commands/feature-plan.md`

**Описание**:
Команда `/feature-plan` минимально документирована по сравнению с `/plan`:

| Аспект | /plan | /feature-plan |
|--------|-------|---------------|
| Выходной путь | `docs/architecture/` | `docs/plans/` |
| Шаблон плана | Есть в architect.md | Нет шаблона |
| Пример | Есть | Нет |
| Инструкции | Подробные | Минимальные |

**Отсутствует**:
- Шаблон документа плана фичи
- Описание структуры плана
- Примеры использования
- Связь с существующей архитектурой

**Влияние на пайплайн**:
- Архитектор не имеет чётких инструкций для режима FEATURE
- Планы фич могут быть несогласованы с основной архитектурой
- Неясно, как интегрировать фичу в существующий проект

---

### P-009: Неясная механика качественных ворот

**Тип**: Неполнота документации
**Файлы**: `workflow.md`, `CLAUDE.md`

**Описание**:
Документация описывает ворота, но не определяет механику их проверки:

**Вопросы без ответа**:
1. **Кто проверяет ворота?**
   - AI-агент автоматически?
   - Система через hooks?
   - Пользователь вручную?

2. **Как выполняется проверка?**
   - Чек-лист в промпте?
   - Автоматический скрипт?
   - Валидация артефактов?

3. **Как фиксируется прохождение?**
   - Файл состояния?
   - Комментарий в артефакте?
   - Только в чате?

**Таблица ворот (workflow.md, строки 414-420)**:
```
| Ворота | Требует подтверждения |
| PRD_READY | Нет (автоматическая) |
| PLAN_APPROVED | ДА (пользователь) |
```

Но: Как реализована "автоматическая проверка"? Где код/hook?

**Примечание в CLAUDE.md (строки 151-152)**:
```
> Качественные ворота — это концепция фреймворка, а НЕ встроенная функция Claude Code.
> Проверка ворот реализуется через инструкции в промптах команд.
```

**Влияние на пайплайн**:
- Ворота могут быть пропущены
- Нет гарантии последовательного прохождения
- Нет истории прохождения ворот

---

### P-010: Конфликт settings.json с командами

**Тип**: Конфликт документов
**Файлы**: `.claude/settings.json`, `.claude/commands/*.md`

**Описание**:
Глобальные разрешения в `settings.json` конфликтуют с локальными в командах:

**settings.json (permissions.allow)**:
```json
"Bash(git :*)",
"Bash(make :*)",
"Bash(docker :*)",
"Bash(pytest :*)",
"Bash(ls :*)",
"Bash(mkdir :*)"
```

**commands/generate.md (allowed-tools)**:
```
Bash(make :*), Bash(docker :*), Bash(pytest :*)
```

**Отсутствуют в generate.md**:
- `Bash(mkdir :*)` — нужен для создания структуры
- `Bash(ls :*)` — нужен для проверки существования

**commands/deploy.md (allowed-tools)**:
```
Bash(make :*), Bash(docker :*), Bash(curl :*)
```

**Отсутствует в settings.json**:
- `Bash(curl :*)` — не разрешён глобально

**Влияние на пайплайн**:
- Непредсказуемое поведение разрешений
- Возможные ошибки доступа
- Неясно, какой уровень разрешений применяется

---

### P-023: Несогласованность между .claude/agents/ и roles/

**Тип**: Конфликт документов
**Файлы**: `.claude/agents/*.md`, `roles/*/*.md`

**Описание**:
Агенты ссылаются на файлы в roles/, но **нет проверки их наличия**:

**Пример из analyst.md (строки 132-136)**:
```markdown
| `roles/analyst/initialization.md` | Детали инициализации |
| `roles/analyst/prompt-validation.md` | Валидация промпта |
| `roles/analyst/requirements-gathering.md` | Сбор требований |
| `roles/analyst/prd-formation.md` | Формирование PRD |
| `docs/prd/template.md` | Шаблон PRD |  ← НЕ СУЩЕСТВУЕТ!
```

**Проверка ссылок analyst.md**:
```
✓ roles/analyst/initialization.md        — существует
✓ roles/analyst/prompt-validation.md     — существует
✓ roles/analyst/requirements-gathering.md — существует
✓ roles/analyst/prd-formation.md         — существует
❌ docs/prd/template.md                   — НЕ СУЩЕСТВУЕТ
```

**Проблемные ссылки в других агентах**:
| Агент | Битая ссылка |
|-------|--------------|
| architect.md | `knowledge/architecture/` — частично |
| implementer.md | `knowledge/services/` — частично |
| validator.md | `knowledge/infrastructure/docker-compose.md` — НЕ СУЩЕСТВУЕТ |

**Влияние на пайплайн**:
- Агенты ссылаются на несуществующие инструкции
- Неполные инструкции приводят к ошибкам
- Нет валидации ссылок в CI/CD

---

### P-024: Нет карты зависимостей шаблонов (Template Dependency Map)

**Тип**: Неполнота документации
**Файлы**: `templates/services/`, `templates/infrastructure/`

**Описание**:
Шаблоны существуют, но **нет документа описывающего их взаимосвязи**:

**Структура templates/**:
```
templates/
├── services/
│   ├── fastapi_business_api/    ← Когда использовать?
│   ├── aiogram_bot/             ← Зависит от чего?
│   ├── asyncio_worker/          ← Требует что?
│   ├── postgres_data_api/       ← Обязателен?
│   └── mongo_data_api/          ← Альтернатива postgres?
├── shared/                      ← Что должно быть везде?
└── infrastructure/              ← Порядок создания?
```

**Отсутствующая документация**:
```
❌ templates/README.md           — Нет общего описания
❌ templates/DEPENDENCIES.md     — Нет карты зависимостей
❌ knowledge/templates-guide.md  — Нет руководства
```

**Вопросы без ответа**:
1. Какие шаблоны обязательны для каждого типа проекта?
2. В каком порядке использовать шаблоны?
3. Какие файлы из shared/ нужны везде?
4. Как адаптировать плейсхолдеры?

**Влияние на пайплайн**:
- Реализатор не знает какие шаблоны использовать
- Возможны пропуски обязательных компонентов
- Несогласованность между проектами

---

### P-025: Пайплайн FEATURE не детализирован

**Тип**: Неполнота пайплайна
**Файлы**: `workflow.md`, `.claude/commands/feature-plan.md`

**Описание**:
Режим FEATURE упоминается, но **детализация минимальна**:

**Сравнение документации CREATE vs FEATURE**:

| Аспект | CREATE | FEATURE |
|--------|--------|---------|
| Описание процесса | Полное (workflow.md:50-54) | 4 строки (workflow.md:56-67) |
| Команда планирования | `/plan` — детальное | `/feature-plan` — минимальное |
| Шаблон плана | docs/templates/architecture-template.md | ❌ НЕТ |
| Интеграция с существующим кодом | N/A | ❌ Не описана |
| Обновление артефактов | N/A | ❌ Не описано |

**Неотвеченные вопросы для FEATURE**:
1. Как обновить существующий docker-compose.yml?
2. Как добавить эндпоинты к существующему API?
3. Как обновить RTM при добавлении требований?
4. Как не сломать существующие тесты?
5. Как переиспользовать shared/ компоненты?

**Влияние на пайплайн**:
- Фичи создаются изолированно от основного кода
- Дублирование инфраструктуры
- Конфликты при интеграции

---

### P-026: Нет проверки предусловий команд

**Тип**: Целостность пайплайна
**Файлы**: `.claude/commands/*.md`

**Описание**:
Команды объявляют предусловия, но **нет механизма их проверки**:

**Пример из generate.md**:
```markdown
## Предусловия
| Ворота | Требование |
|--------|------------|
| `PLAN_APPROVED` | План утверждён пользователем |
```

**Проблема**: Пользователь может запустить `/generate` БЕЗ плана:
```bash
/idea "проект"
# Пользователь НЕ запускает /research и /plan
/generate  ← Что произойдёт? Нет плана!
```

**Необходимый механизм (отсутствует)**:
```python
# Hook или проверка в команде:
def check_prerequisites(command: str) -> bool:
    if command == "/generate":
        plan = find_artifact("docs/architecture/*-plan.md")
        if not plan:
            raise PrerequisiteError("PLAN_APPROVED not met")
        if not is_approved(plan):
            raise PrerequisiteError("Plan not approved by user")
    return True
```

**Влияние на пайплайн**:
- Возможен запуск этапов без предусловий
- Ворота обходятся
- Результат непредсказуем

---

## 🟡 MEDIUM: Проблемы среднего приоритета

### P-011: Отсутствие документации по интеграции фич

**Тип**: Неполнота документации
**Файлы**: `workflow.md`, `roles/implementer/*.md`

**Описание**:
Документация не описывает, как интегрировать новую фичу в существующий проект:

**Неотвеченные вопросы**:
- Как обновить существующий `docker-compose.yml`?
- Как добавить сервис к существующей инфраструктуре?
- Как обновить RTM при добавлении требований?
- Как переиспользовать shared/ компоненты?
- Как обновить CI/CD?

**Влияние на пайплайн**:
- Фичи могут создаваться изолированно
- Дублирование инфраструктуры
- Несогласованность между сервисами

---

### P-012: Нет документации по структуре шаблонов

**Тип**: Неполнота документации
**Файлы**: `templates/services/*/`, `templates/infrastructure/`

**Описание**:
Шаблоны существуют, но не документированы:

| Шаблон | README | Инструкции |
|--------|--------|------------|
| `fastapi_business_api/` | Есть | Минимальные |
| `aiogram_bot/` | Есть | Минимальные |
| `asyncio_worker/` | Есть | Минимальные |
| `postgres_data_api/` | Есть | Минимальные |
| `mongo_data_api/` | Есть | Минимальные |
| `infrastructure/` | Есть | Минимальные |

**Отсутствует**:
- Какие файлы обязательны, какие опциональны?
- Как адаптировать шаблон под конкретный проект?
- Какие плейсхолдеры нужно заменить?
- Зависимости между шаблонами

**Влияние на пайплайн**:
- Реализатор может неправильно использовать шаблоны
- Пропуск обязательных файлов
- Несогласованность между сервисами

---

### P-013: Философия "Артефакты = Память" без реализации

**Тип**: Неполнота документации
**Файлы**: `workflow.md`

**Описание**:
В `workflow.md` (строка 6) заявлена философия:
```
**Философия**: Артефакты = Память. Не полагаемся на память чата.
```

**Неотвеченные вопросы**:
- Как агент получает доступ к артефактам предыдущих этапов?
- Где хранится состояние пайплайна?
- Как передаётся контекст между этапами?
- Как агент узнаёт, какие ворота уже пройдены?

**Влияние на пайплайн**:
- Агенты могут потерять контекст
- Повторение уже выполненной работы
- Несогласованность между этапами

---

### P-014: Несогласованность именования файлов

**Тип**: Несогласованность
**Файлы**: `conventions.md`, `docs/templates/`

**Описание**:
Соглашения указывают разные стили именования:

**conventions.md (строки 28-31)**:
```
| Markdown | kebab-case.md | api-contracts.md |
| Конфиги | kebab-case | docker-compose.yml |
```

**Реальные файлы в docs/templates/**:
```
prd-template.md           ← kebab-case ✓
architecture-template.md  ← kebab-case ✓
qa-report-template.md     ← kebab-case ✓
```

**Но в roles/**:
```
roles/analyst/prd-formation.md      ← kebab-case ✓
roles/implementer/data-service.md   ← kebab-case ✓
```

**Несогласованность**:
Соглашение: `kebab-case.md`
Некоторые файлы: `snake_case` в пути (`roles/analyst/`)

**Влияние на пайплайн**:
- Путаница при создании новых файлов
- Несогласованность в именовании

---

### P-015: Отсутствие версионирования артефактов

**Тип**: Неполнота документации
**Файлы**: `workflow.md`

**Описание**:
Документация не описывает, как версионировать артефакты:

- Что если PRD изменился после утверждения плана?
- Как отслеживать изменения в архитектурном плане?
- Нужна ли ревизия артефактов?
- Как синхронизировать RTM с изменениями?

**Влияние на пайплайн**:
- Устаревшие артефакты
- Несоответствие между документами и кодом
- Потеря истории изменений

---

### P-027: RTM не имеет шаблона

**Тип**: Неполнота документации
**Файлы**: `.claude/agents/validator.md`, `docs/templates/`

**Описание**:
Валидатор должен создать RTM (Requirements Traceability Matrix), но **шаблон отсутствует**:

**Из validator.md (строки 81-100)**:
```markdown
### 3. Обновление RTM
Создать/обновить `docs/rtm.md`:
```

**Проверка шаблонов**:
```
docs/templates/
├── prd-template.md           ← Есть
├── architecture-template.md  ← Есть
├── qa-report-template.md     ← Есть
├── tasklist-template.md      ← Есть
❌ rtm-template.md             ← НЕТ
```

**Формат RTM в validator.md не стандартизирован**:
- Только пример в markdown
- Нет обязательных полей
- Нет валидации структуры

**Влияние на пайплайн**:
- Каждый RTM может иметь разную структуру
- Нет автоматической валидации
- Сложность сравнения между проектами

---

### P-028: Нет версионирования артефактов

**Тип**: Неполнота пайплайна
**Файлы**: `workflow.md`

**Описание**:
Артефакты создаются без механизма **версионирования**:

**Сценарий проблемы**:
```bash
# День 1:
/idea "проект"     → docs/prd/booking-prd.md (v1)
/plan             → docs/architecture/booking-plan.md (v1)
/generate         → services/... (на основе plan v1)

# День 5 (новые требования):
/idea "добавить фичу"  → docs/prd/booking-prd.md ПЕРЕЗАПИСАН (v2)
                       → План v1 устарел!
                       → Код не соответствует PRD v2!
```

**Отсутствующие механизмы**:
| Механизм | Статус |
|----------|--------|
| Версии в имени файла | ❌ Нет `booking-prd-v2.md` |
| Git-версионирование | ❌ Не описано в процессе |
| Хеши артефактов | ❌ Нет отслеживания изменений |
| Changelog в файле | ❌ Нет секции "История изменений" |

**Влияние на пайплайн**:
- Устаревшие артефакты не обнаруживаются
- Код может не соответствовать документации
- Нет аудит-трейла изменений

---

### P-029: Отсутствует диаграмма зависимостей файлов (Navigation Map)

**Тип**: Неполнота документации
**Файлы**: Весь проект

**Описание**:
Нет документа показывающего **что от чего зависит**:

**Вопросы AI при чтении проекта**:
1. С какого файла начать? (CLAUDE.md?)
2. После CLAUDE.md что читать? (conventions.md?)
3. Какой агент какие файлы использует?
4. Какая команда какие артефакты создаёт?

**Попытка навигации в CLAUDE.md**:
```markdown
## Порядок чтения документации
1. CLAUDE.md (этот файл)
2. conventions.md
3. workflow.md
4. .claude/agents/{role}.md
5. roles/{role}/*.md
6. knowledge/{topic}/
```

**Проблема**: Это только порядок чтения, не полная карта зависимостей.

**Необходимая диаграмма (отсутствует)**:
```
CLAUDE.md
    ├── → conventions.md
    ├── → workflow.md
    │       ├── → .claude/commands/*.md
    │       └── → .claude/agents/*.md
    │               └── → roles/*/*.md
    ├── → knowledge/*/*.md
    └── → templates/*
            └── → shared/
```

**Влияние на пайплайн**:
- AI не понимает полную структуру
- Возможен пропуск важных файлов
- Неэффективная навигация

---

### P-030: Конфликт пути tasklists/

**Тип**: Несогласованность
**Файлы**: `workflow.md`, `docs/templates/tasklist-template.md`

**Описание**:
workflow.md упоминает `docs/tasklists/`, но:

**Из workflow.md (строки 315-316)**:
```
docs/
├── tasklists/
│   └── {name}-tasks.md
```

**Реальность**:
```
❌ docs/tasklists/       — НЕ СУЩЕСТВУЕТ
✓ docs/templates/tasklist-template.md — СУЩЕСТВУЕТ
```

**Несогласованность**:
1. Шаблон есть, но директория не создаётся
2. Ни один агент не создаёт tasklists/
3. Ни одна команда не генерирует {name}-tasks.md
4. В agent.md нет ссылок на tasklists

**Влияние на пайплайн**:
- Шаблон tasklist-template.md не используется
- Мёртвый код в документации
- Путаница в структуре

---

## 🟢 LOW: Проблемы низкого приоритета

### P-016: Строгое требование Python 3.11+

**Тип**: Ограничение
**Файлы**: `README.md`

**Описание**:
README указывает (строка 36):
```
- Python 3.11+
```

Но большинство зависимостей совместимы с Python 3.10+:
- FastAPI
- Pydantic v2
- SQLAlchemy
- pytest

**Влияние**: Ограничение аудитории.

---

### P-017: Ссылка на несуществующий раздел agents в CLAUDE.md

**Тип**: Косметическое
**Файлы**: `CLAUDE.md`

**Описание**:
CLAUDE.md (строка 240) ссылается на:
```
| [.claude/agents/](/.claude/agents/) | Определения AI-ролей |
```

Путь с `/` в начале может не работать в некоторых системах.

**Влияние**: Битая ссылка в GitHub.

---

### P-018: Избыточные пути с "tasklists/"

**Тип**: Несогласованность
**Файлы**: `workflow.md`

**Описание**:
workflow.md (строки 315-316) указывает:
```
docs/tasklists/
└── {name}-tasks.md
```

Но нигде больше `tasklists/` не упоминается:
- Нет шаблона
- Нет инструкций по созданию
- Нет ссылок в агентах

**Влияние**: Путаница, возможный мёртвый код в документации.

---

### P-031: Нет отдельного агента Deployer

**Тип**: Архитектурное решение
**Файлы**: `.claude/agents/validator.md`, `.claude/commands/deploy.md`

**Описание**:
Команда `/deploy` использует агента **Валидатор**, хотя деплой — отдельная функция:

**Из deploy.md**:
```markdown
## Агент
**Валидатор** (`.claude/agents/validator.md`)
```

**Из validator.md**:
```markdown
> **Назначение**: Финальная проверка всех артефактов и качественных ворот.
> Седьмой и восьмой этапы пайплайна AIDD-MVP.
```

**Проблема**: Валидатор имеет две разные ответственности:
1. Валидация (этап 7) — проверка ворот и артефактов
2. Деплой (этап 8) — сборка и запуск контейнеров

**Нарушение принципа Single Responsibility**:
| Функция | Инструменты | Ответственность |
|---------|-------------|-----------------|
| Валидация | Read, Grep | Проверка документов |
| Деплой | Bash(docker), Bash(curl) | Операции с инфраструктурой |

**Влияние**:
- Смешение ответственностей
- Избыточные инструменты для одного агента
- Путаница при расширении функционала

---

## Статистика по категориям

### По типу проблемы

| Тип | Количество |
|-----|------------|
| Целостность структуры | 3 |
| Целостность пайплайна | 5 |
| Битые ссылки | 2 |
| Конфликт документов | 5 |
| Неполнота пайплайна | 5 |
| Дублирование информации | 1 |
| Неполнота документации | 8 |
| Несогласованность | 2 |
| Архитектурное решение | 1 |

### По затронутым файлам

| Файл | Количество проблем |
|------|-------------------|
| `workflow.md` | 12 |
| `.claude/agents/*.md` | 8 |
| `.claude/commands/*.md` | 7 |
| `CLAUDE.md` | 3 |
| `.claude/settings.json` | 2 |
| `templates/` | 3 |
| `docs/templates/` | 2 |

---

## Связи между проблемами

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         ГРАФ ЗАВИСИМОСТЕЙ ПРОБЛЕМ                            │
├─────────────────────────────────────────────────────────────────────────────┤

P-001 (Несуществующие директории)
    ↓ блокирует
P-002 (Неправильный путь к шаблону)
    ↓ влияет на
P-022 (Нет bootstrap процесса)
    ↓ требует
P-019 (Нет индекса файлов)

P-003 (Конфликт инструментов agents/commands)
    ↓ связан с
P-010 (Конфликт settings.json)
    ↓ влияет на
P-026 (Нет проверки предусловий)

P-004 (Нет отката при неудаче)
    ↓ связан с
P-009 (Неясная механика ворот)
    ↓ связан с
P-020 (Нет передачи состояния)

P-006 (Нет алгоритма CREATE/FEATURE)
    ↓ связан с
P-025 (FEATURE не детализирован)
    ↓ влияет на
P-011 (Нет документации интеграции фич)

P-019 (Нет индекса файлов) ←──────────────────┐
    ↓ блокирует                                │
P-021 (Нет алгоритма обнаружения артефактов)   │
    ↓ влияет на                                │
P-020 (Нет передачи состояния) ────────────────┘

P-024 (Нет карты шаблонов)
    ↓ влияет на
P-012 (Нет документации структуры шаблонов)

P-015 (Нет версионирования)
    ↓ связан с
P-028 (Нет версионирования артефактов)

P-023 (Несогласованность agents/roles)
    ↓ влияет на
P-029 (Нет карты зависимостей файлов)

=== НОВЫЕ КРИТИЧЕСКИЕ ПРОБЛЕМЫ ===

P-032 (Дублирование фреймворков) ←─────────────────────────────────────┐
    ↓ является причиной                                                 │
P-033 (7 vs 8 стадий)                                                  │
    ↓ связан с                                                          │
P-034 (docs/ vs ai-docs/)                                              │
    ↓ влияет на                                                         │
P-035 (.ai-framework не интегрирован) ─────────────────────────────────┤
    ↓ блокирует                                                         │
P-036 (Navigation Matrix не адаптирована) ←────────────────────────────┘
    ↓ является решением для
P-019 (Нет индекса файлов)
P-020 (Нет передачи состояния)
P-021 (Нет алгоритма обнаружения артефактов)

КОРНЕВАЯ ПРОБЛЕМА:
P-032 → Вся критическая документация УЖЕ СУЩЕСТВУЕТ в .ai-framework/
        но НЕ ИСПОЛЬЗУЕТСЯ основным проектом

└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Диаграммы пайплайнов и решение

> **ВНИМАНИЕ**: Детальные диаграммы пайплайнов вынесены в отдельный документ для избежания дублирования.

### Ссылка на детальный анализ

**Полный анализ проблемы интеграции пайплайнов**: [2025-12-20-pipeline-integration-problem.md](2025-12-20-pipeline-integration-problem.md)

Документ содержит:
- Детальные диаграммы пайплайнов CREATE и FEATURE
- Пайплайны для каждой из 9 slash-команд (/idea, /research, /plan, /feature-plan, /generate, /review, /test, /validate, /deploy)
- Сравнение фреймворков AIDD-MVP (8 стадий) vs .ai-framework (7 стадий)
- Оценку совместимости (70%)
- Три варианта решения с рекомендацией
- План действий с чек-листом

### Краткая сводка решения

| Вариант | Описание | Сложность |
|---------|----------|-----------|
| A. Полная интеграция | Объединить оба фреймворка в единый | ⭐⭐⭐⭐⭐ |
| B. .ai-framework как расширение | Использовать только для Maturity 3-4 | ⭐⭐ |
| **C. Извлечение полезного** | Создать INDEX.md, Navigation Matrix (РЕКОМЕНДУЕТСЯ) | ⭐⭐⭐ |

---

**Версия документа**: 4.0
**Создан**: 2025-12-20
**Обновлён**: 2025-12-21
**Автор**: AI Agent (Анализатор)

**Изменения v4.0**:
- Удалено дублирование: диаграммы пайплайнов и раздел "Главное открытие" вынесены в отдельный файл
- Добавлена ссылка на [2025-12-20-pipeline-integration-problem.md](2025-12-20-pipeline-integration-problem.md)
- Сокращён размер документа на ~290 строк

**Изменения v3.0**:
- Добавлены проблемы P-032 — P-036 (дублирование фреймворков)
- Добавлен раздел "Главное открытие" с найденным решением
- Обновлён граф зависимостей с корневой проблемой
